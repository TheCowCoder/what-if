# Copilot State Tracker

Original roadmap
- Repulsive minimap markers with accurate anchors and no grouped indicators.
- Cross-instance player-presence consistency, including the red Target player buttons after movement.
- Paged Settings with a Debug page and unlimited turn times.
- Non-exploration arena PvP moving toward the same map-driven backend as exploration combat.
- Arena preview flow: 30s avatar/profile review, then a one-pass 2 minute rewrite window, then battle.
- Iterative validation with incremental commits and explicit user testing.

Audit status as of 2026-04-08

Completed
- Minimap markers now use repelled label/icon layout on both minimap views while keeping anchor positions accurate.
- Settings has a General page and a Debug page.
- Unlimited turn times is wired through arena queue rooms and bot matches.
- Non-exploration arena and bot fights now spawn into room-backed world-map state and use the battle minimap.
- Arena rooms now enter a preparation flow before battle: 30 second profile review, then a 2 minute rewrite window, then the duel starts automatically.
- Profile inspection from circular avatars is live through the existing markdown modal.
- The prep inspect modal now closes when the read phase ends, and opponent profiles cannot be reopened during the write phase.
- Typing indicators are now shared state instead of one-shot pings.
- Battle turn-status chips show animated typing dots.
- Arena preparation avatars show animated typing dots beneath other players while they rewrite.
- Exploration nearby-player target buttons show animated typing dots.
- Bot matches now rewrite the bot markdown during the prep write phase.
- Character sync payloads now strip portrait image data before socket and cloud sync to avoid 413 payload failures.
- Character avatars now persist safely in MongoDB without bloating the main character sync payload; avatars upload separately and stripped syncs preserve existing portraits.
- Auto portrait generation no longer runs during the arena prep markdown rewrite phase.
- Arena prep rewrite now has authoritative lock-in state, below-avatar typing and locked indicators, and a Pass action for players who want to keep their profile unchanged.
- Bot prep rewrite completion now resolves into the same locked-in state shown to players instead of only flashing a typing indicator.
- Arena prep preview is now 15 seconds instead of 30 and can start early when every human participant votes to skip ahead.
- Bot prep rewrite now has a fallback lock-in path so a failed or empty rewrite does not leave the room hanging.
- Arena battle movement is now shared log-visible state instead of local-only feedback.
- Arena resolution prompts and bot actions now receive map-distance context so chase and flee positioning affect combat logic.
- Battle turn resolution now accepts exact judge-authored x/y position updates, persists them through room map state, and recenters the minimap on live coordinates so mid-water movement is visible.
- Exploration presence now applies authoritative server player updates back onto the local exploration state.

Partially complete
- Arena map parity is now established at the room-state, movement, prompt-context, and dynamic position-update layer, but full exploration-combat rule parity is not complete yet.
- Shared presence handling is improved by preserving transient player state through exploration updates, but the original Target player desync report still needs explicit retesting.

Still open
- Confirm and, if needed, finish the cross-instance Target player consistency bug after movement.
- Implement chase/flee/out-of-bounds arena rules so non-exploration arena combat fully matches the exploration combat model.
- Continue collapsing arena and exploration combat logic toward a cleaner shared backend instead of parallel feature paths.
- Retest the new prep lock/pass flow and dynamic battle-position updates across two clients, especially long swims and pursuit turns.

Current focus
- Verify avatar persistence, preview skip flow, prep lock/pass flow, and dynamic battle-position updates across two clients.
- Finish cross-instance presence consistency if the Target player bug is still reproducible.
- Resume full arena combat parity after the new position pipeline is validated.

Detailed backlog

1. Minimap and presence audit
- Retest marker repulsion and player-target visibility across multiple clients after movement.
- Keep transient player flags like typing and acting stable across world-state refreshes.

2. Arena preparation flow
- Review stage: 30 second avatar/profile inspection for all non-exploration arena rooms.
- Tweak stage: 2 minute one-pass rewrite window using the character architect.
- Keep room player data in sync when a user saves a new revision before battle.
- Preserve explicit prep lock state, including pass-through lock-in when a player chooses not to rewrite.
- Allow the room to skip the preview countdown early once all human players agree.

3. Shared typing indicators
- Room-scoped typing drives arena prep and battle indicators.
- Exploration typing is shown on nearby target chips.
- Retest edge cases around disconnects, unlocks, and room transitions.

4. Arena combat parity
- Keep the map-backed room state as the source of truth.
- Continue tightening chase, flee, and location-based combat outcomes now that prompt context, movement logs, and judge-authored exact positions are live.

5. Bot parity
- Decide whether bots should rewrite their profile during prep and, if yes, add the bot-model-driven rewrite step.

6. Validation discipline
- Rebuild after each batch.
- Commit incrementally.
- Keep asking the user to test before closing remaining roadmap items.

Current known constraints
- Global exploration lock-in is the intended behavior; do not scope lock-in by location.
- Keep the direct move-state update fix that makes the mover's minimap update immediately.
- Keep battle map state authoritative on the server after each resolved turn so judge-authored x/y updates do not snap back next turn.
- Preserve exploration thought cards, battle queued-action preview, and the battle minimap while extending arena systems.
