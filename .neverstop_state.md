# Neverstop State

- Iteration: v3
- Status: active
- Completed:
  - Added island-aware battle image composition rules with split-view enforcement for fighters on different islands.
  - Changed shared model settings hydration to a state-based gate so pre-hydration edits still persist once startup loading completes.
  - Added global 503 retry pills for character creation, exploration, battle resolution, and bot AI retries.
  - Broadcast battle retry status to the full PvP room and started the battle stall timer before awaiting the stream request.
  - Removed the extra green self dots from the map, moved the green target to the exploration spawn point, and added stronger label repulsion against dots/icons.
  - Force-close the full map overlay during reconnect/disconnect transitions to avoid the stuck minimap state.
- Validation:
  - No current editor errors in src/App.tsx
  - No current editor errors in server.ts
- Next:
  - User test of cross-device model sync, minimap behavior after reconnects, battle retry pills in 2-player PvP, and the updated map markers.
